# =================================================== #
#              List of dependencies. 
# =================================================== #

# NOTE: Install it with pip install -r requirements.txt

casadi>=3.7
cvxpy>=1.7
matplotlib>=3.8
numpy>=1.26
scipy>=1.11

# double_integrator_stochastic_traj_opt.py and cr3bp_stochastic_traj_opt_jax.py
# only -- both also require a SNOPT-linked pyoptsparse build.
jax>=0.10.2
diffrax>=0.7
